import CslibHeaders.Basic
import Cslib.Foundations.Syntax.HasSubstitution

/-! # HasSubstitution: substitution notation

  ## Vocabulary
  - `Cslib.HasSubstitution α β γ` — typeclass for substitution
  - `t[x := t']` — notation: replace `x` in `t` with `t'`

  This module defines a notation typeclass; no standalone theorems to export.
-/
