import CslibHeaders.Basic
import Cslib.Foundations.Syntax.Context

/-! # Context: single-hole contexts

  ## Vocabulary
  - `Cslib.HasHContext α β` — class for heterogeneous single-hole contexts
  - `Cslib.HasContext α` — class for homogeneous single-hole contexts
  - `c<[t]>` — fill the hole in context `c` with term `t`

  This module defines typeclasses; no standalone theorems to export.
-/
