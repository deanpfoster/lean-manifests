import CslibHeaders.Basic
import CslibHeaders.Defs.Logics.LinearLogic.CLL.CutElimination

/-! # Cut Elimination for CLL (definitions only)

  Vocabulary (see Defs/):
    Proof.cutFree           — checks whether a proof is cut-free
    CutFreeProof Gamma      — a proof of Gamma without cuts

  Note: Cut admissibility and cut elimination theorems are TODO in the source.
-/
